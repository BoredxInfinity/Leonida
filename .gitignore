__pycache__/
*.py[cod]

.venv/
venv/

.idea/
.vscode/
.DS_Store

# Depth-overlay runtime and model, ~70MB, fetched with --fetch-model
assets/

# This Pi's own certificate. The key especially must never be committed.
cert.pem
key.pem
